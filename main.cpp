#include <cassert>
#include <string>
#include <sstream>

#include "math.h"
#include "BloomFilter.h"
#include "HashTable.h"
#include "time.h"

int main(int argc, char *argv[]) {
    // 1) first, we process the inputs

    // 2) create our BloomFilter and auxilary HashTable based on the inputs

    // 3) print the values we are experimenting on

    // 4) run 10 rounds of tests, each consisting of inserting 1000 elements

    // 5) print the total statistics of our tests

    return 0;
}